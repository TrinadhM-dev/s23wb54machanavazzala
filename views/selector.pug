extends layout

block content
  h1= title
  -let results =["Captain.jpg","hulk.jpg","ironMan.jpg","spiderman.jpg","antman.jpg"]
  -for(var i=0;i<3; i++) {
    -let r = Math.floor(Math.random()*5)
    img(src=`images/${results[r]}`,height="300px",width="300px")
  -}
